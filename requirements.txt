# Core framework
Django>=4.2,<6

# Linting / code quality
flake8>=6.0.0

# Testing
pytest>=7.0.0
pytest-django>=4.5.2

# Database drivers
# (SQLite works out of the box, so nothing required here — 
#  but uncomment psycopg2 if you later switch to Postgres)
# psycopg2-binary>=2.9

# Environment variable support
python-dotenv>=1.0.0

# Optional: formatting tools (can help with linting)
black>=23.0
isort>=5.12
